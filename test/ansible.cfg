[defaults]
# Look for roles one level up (the project root contains elasticsearch_cluster/)
roles_path  = ../
inventory   = hosts.ini
# Suppress deprecation noise during testing
deprecation_warnings = false
